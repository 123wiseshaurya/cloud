# =====================
# 📦 Kafka (Railway Internal)
# =====================
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka.railway.internal:29092}

# ✅ Producer config
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# ✅ Consumer config
spring.kafka.consumer.group-id=job-scheduler-group
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.listener.missing-topics-fatal=false
spring.kafka.consumer.auto-offset-reset=earliest

# ✅ Kafka Admin config (for auto topic creation)
spring.kafka.admin.auto-create=true
spring.kafka.admin.fail-fast=false

# ✅ Optional: Enable Kafka debug logging (good for Railway debugging)
logging.level.org.springframework.kafka=DEBUG
logging.level.org.apache.kafka=DEBUG
